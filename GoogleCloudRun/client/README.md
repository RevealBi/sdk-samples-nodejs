# GoogleCloudRun client

Use the same client found in the GettingStarted sample, changing the following line in index.html as appropriate:
``
        $.ig.RevealSdkSettings.setBaseUrl("http://localhost:8080/reveal-api/");
``